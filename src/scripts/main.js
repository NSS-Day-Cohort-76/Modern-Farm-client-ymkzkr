import { addPlant, usePlants } from "./field.js"
import { createPlan } from "./plan.js"



const yearlyPlan = createPlan()
console.log(yearlyPlan)




// passes certain seed objects into the usePlants array
const addAsparagus = usePlants(asparagusSeed)
console.log(addAsparagus)
const addCorn = usePlants(cornSeed)
console.log(addCorn)
const addWheat = usePlants(wheatSeed)
console.log(addWheat)
const addAsparagus = usePlants(asparagusSeed)
console.log(addAsparagus)
const addAsparagus = usePlants(asparagusSeed)
console.log(addAsparagus)
const addAsparagus = usePlants(asparagusSeed)
console.log(addAsparagus)